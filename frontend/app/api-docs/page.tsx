export default function ApiDocsPage() {
  return (
    <div>
      <h1>API Docs</h1>
      <p>Endpoints: /api/questions/flagged, /api/questions/:id/audit, /api/domains/calibration, /api/forecaster/:username/blind-spots.</p>
    </div>
  );
}
